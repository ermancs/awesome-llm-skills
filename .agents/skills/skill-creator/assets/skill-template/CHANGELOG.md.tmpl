# Changelog

All notable changes to the **{{NAME}}** skill. Format:
[Keep a Changelog](https://keepachangelog.com/); versioning: SemVer.

## [0.1.0] — {{DATE}}
### Added
- Scaffolded from the 7-pillar `skill-creator` standard.

### TODO before first real use
- Replace every `[[FILL: …]]` placeholder in SKILL.md.
- Implement `scripts/run.py` (or delete if no deterministic step is needed).
- Write real eval prompts + assertions in `evals/evals.json`.
- Fill the canonical output shape in `assets/templates/output.md`.
